#DEV environment

app:
  message: This is the property file fo the  ${spring.application.name} specific to DEV environment

spring:
  datasource:
     driver-class-name: org.h2.Driver
     url: jdbc:h2:men:db;DB_CLOSE_DEPLAY=-1
     username: sa
     password: sa
